<td>
  <p>
    <span><%= model.good.name %></span>
    <span class="has-text-grey">（<%= model.good_id %>）</span>
  </p>
  <p><%= model.aim_i18n %></p>
</td>
<td><%= model.number %></td>
<td>
  <p>
    <span><%= model.user.name %></span>
    <span class="has-text-grey">（<%= model.user_id %>）</span>
  </p>
  <p>
    <span><%= model.address.area.full_name %></span>
    <span><%= model.address.detail %></span>
    <span class="has-text-grey">（<%= model.address_id %>）</span>
  </p>
</td>
<td><%= model.produce_plan&.title %></td>
<td><%= model.status_i18n %></td>
