<div class="ui modal">
  <i class="fas fa-times close"></i>
  <div class="ui header">
    <%= t('.edit') %>>
  </div>

  <div class="content">
    <%= form_with model: [:admin, @address] do |f| %>
      <%= render partial: 'form', locals: { f: f } %>
    <% end %>
  </div>
</div>
