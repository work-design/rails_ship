<table class="table is-striped is-fullwidth as-boxed">
  <thead>
    <tr>
      <%= render 'package_thead' %>
      <%= render 'index_thead_actions' %>
    </tr>
  </thead>
  <tbody id="package_tbody">
    <%= yield %>
  </tbody>
</table>
