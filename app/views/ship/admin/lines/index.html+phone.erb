<%= render 'filter_form' %>
<%= render 'filter_table' %>

<%= render layout: 'index_table' do %>
  <%= render partial: 'index_tbody', layout: 'index_tr', collection: @lines, as: :model %>
<% end %>

<%= paginate @lines %>
