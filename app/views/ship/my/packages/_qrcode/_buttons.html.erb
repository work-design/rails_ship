<div data-controller="scan">
  <div class="buttons are-large is-centered">
    <%= button_to({ controller: 'ship/my/packages', action: 'receive', id: @package.id }, class: 'button is-success') do %>
      <span>提货</span>
    <% end if ['unloaded'].include?(@package.state)  %>
  </div>
</div>
