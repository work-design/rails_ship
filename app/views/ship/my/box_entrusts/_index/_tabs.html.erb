<div class="tabs is-centered">
  <ul>
    <li class="is-active">
      <%= link_to({ action: 'index' }) do %>
        <span>买入</span>
      <% end %>
    </li>
    <li>
      <%= link_to({ action: 'sell' }) do %>
        <span>卖出</span>
      <% end %>
    </li>
  </ul>
</div>

<div class="is-flex is-justify-content-space-between px-4">
  <div>
    <span>最大可买：</span>
    <span><%=  %></span>
  </div>
  <div>
    <span>可用金额：</span>
    <span class="has-text-danger"><%= current_user.lawful_wallet.amount.to_money.format(html_wrap: true).html_safe %></span>
  </div>
</div>
