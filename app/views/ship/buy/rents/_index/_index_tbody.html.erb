<td>
  <p><%= model.rentable_type_i18n %></p>
</td>
<td>
  <p><time data-controller="time"><%= model.start_at.to_fs(:iso8601) %></time></p>
  <p><time data-controller="time"><%= model.estimate_finish_at&.to_fs(:iso8601) %></time></p>
</td>
<td><time data-controller="time"><%= model.finish_at&.to_fs(:iso8601) %></time></td>
<td>
<% if model.duration.zero? %>
  <time data-controller="count-up" data-count-up-time-value="<%= model.start_at.to_fs(:iso8601) %>" class="has-text-mono"><%= exact_distance_time(model.start_at, Time.current) %></time>
<% else %>
  <%= model.duration.inspect %>
<% end %>
</td>
<td>
  <%= link_to({ action: 'promote', id: model.id }, data: { turbo_frame: 'modal' }) do %>
    <i class="fa-light fa-seal-question"></i>
  <% end %>
  <span class="pl-1"><%= model.price.to_money.format(html_wrap: true).html_safe %></span>
</td>
