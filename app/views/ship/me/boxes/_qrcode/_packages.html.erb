<table class="table is-striped is-borderless is-fullwidth">
  <thead>
    <tr class="text-weight-normal size-7">
      <th><%= Ship::Package.human_attribute_name(:id) %></th>
      <th><%= Ship::Package.human_attribute_name(:boxed_in_at) %></th>
    </tr>
  </thead>
  <%= render partial: 'box_log', collection: @box.box_logs.order(id: :desc), as: :model %>
</table>
